# Vibey Development Notes

## Testing in Browser

When testing the app in a browser via DevTools and you need to authenticate with OpenRouter:
- Ask the user to help log in, as OAuth flows require user interaction
- The user can click "Sign in with OpenRouter" and complete the authentication
